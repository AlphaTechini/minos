# Public Deployment Configuration

To find the server route that reads selected Vercel environment values and returns only public Minos contract configuration to the browser, visit [+server.js](file:///C:/Hackathons/Proof%20Firewall/src/routes/api/deployment/+server.js).

Private keys are intentionally excluded from the response.
