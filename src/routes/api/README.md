# Dashboard API Routes

This directory contains server routes used by the Minos dashboard. Each route selects only the minimum public data needed by the browser and must never return deployment, worker, or source-owner private keys.

To find the public deployment configuration route, visit [deployment/README.md](file:///C:/Hackathons/Proof%20Firewall/src/routes/api/deployment/README.md).
