# Dashboard Contract Client

To find read-only evidence loading, wallet network switching, and user-authorized source and Creditcoin actions, visit [contracts.js](file:///C:/Hackathons/Proof%20Firewall/src/lib/contracts.js).

The client accepts public deployment configuration from `static/deployment.json` and never reads server-side secrets.
